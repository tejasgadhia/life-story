import { TABS } from './tabs'

// Tab slugs derived from centralized tab config
export const TAB_SLUGS = TABS.map(tab => tab.slug)

// Valid year range for birth dates
export const MIN_YEAR = 1946
export const MAX_YEAR = 2012
