/**
 * Font loading — no-op.
 *
 * All fonts (Inter + Newsreader) are preloaded in index.html as critical fonts.
 * This export is kept so existing imports in ThemeWrapper.jsx don't break.
 */
export function loadReportFonts() {}
